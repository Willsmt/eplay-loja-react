import styled from 'styled-components'
import { colors } from '../../styles'

export const Container = styled.div`
  text-align: center;
  padding: 40px 0;
  color: ${colors.white};
  font-size: 18px;
  font-weight: bold;
`
